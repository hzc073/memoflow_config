# Tasks: optimize-config-webpage

## 1. 问题收集

- [x] 1.1 记录 config 网页使用中的具体问题、影响范围和期望行为。
- [x] 1.2 将已确认的问题归类为 UI 布局、交互流程、数据校验、发布流程、错误提示或测试覆盖。
- [x] 1.3 根据确认后的问题清单补充 `proposal.md` 和实现任务。

## 2. 设计图

- [x] 2.1 使用 Pencil 生成总体改版效果图。
- [x] 2.2 生成 `数据面板`、`版本发布`、`内容维护`、`系统设置` 四个主页面效果图。
- [x] 2.3 根据反馈精简侧边栏和按钮数量，并将 AI 翻译并入版本发布向导。

## 3. 实现

- [x] 3.1 将侧边栏改为 4 个主入口，并用内部标签承载低频功能。
- [x] 3.2 数据面板取消右侧配置状态，按最新版本优先排序下载量。
- [x] 3.3 版本发布向导改为单栏，增加 `保存草稿`，将预览集中到第 5 步。
- [x] 3.4 将 AI 翻译入口并入版本发布向导，并为人工审核增加上一个/下一个语言切换。
- [x] 3.5 将 `校验`、`构建` 等文案调整为更通俗的操作说明。
- [x] 3.6 为长下载链接和生成摘要输出补充换行约束，避免撑破容器。

## 4. 验证

- [x] 4.1 运行 `node --check config/manager.js`。
- [x] 4.2 运行 `python -m unittest discover -s config -p "test_*.py"`。
- [x] 4.3 启动本地配置管理器，并使用 Playwright/Edge 导出四个实际页面截图。
